import React from 'react'

const useroutput = (props) => {

    return(
        <div>
            <p>{props.username}</p>
            <p>{props.username}</p>
        </div>
        
    );
}

export default useroutput;